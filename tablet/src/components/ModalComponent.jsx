import {
  Dialog,
  AppBar,
  Toolbar,
  IconButton,
 
  Container,
} from "@mui/material";

export const ModalComponent = ({ openCreateRole }) => {
  return (
    <>
    <div>hola</div>
     
    </>
  );
};
